# Миграция: Добавление таблицы search_bots

## Описание изменений

Добавлена новая таблица `search_bots` для отслеживания поисковых ботов отдельно от реальных пользователей.

## Что нужно сделать на сервере

### Вариант 1: Автоматическое создание (рекомендуется)

Таблица создастся автоматически при следующем запуске приложения, если используется `db.create_all()`.

**Шаги:**
1. Остановить приложение на сервере
2. Обновить код с GitHub:
   ```bash
   cd /path/to/docscan-ai
   git pull origin main
   ```
3. Перезапустить приложение
4. Таблица `search_bots` создастся автоматически

### Вариант 2: Ручное создание через скрипт

Если нужно создать таблицу вручную:

**Шаги:**
1. Подключиться к серверу по SSH
2. Перейти в директорию проекта:
   ```bash
   cd /path/to/docscan-ai
   ```
3. Обновить код:
   ```bash
   git pull origin main
   ```
4. Запустить скрипт создания БД:
   ```bash
   python3 create_database.py
   ```
5. Проверить что таблица создана:
   ```bash
   sqlite3 docscan.db ".tables"
   ```
   Должна появиться таблица `search_bots`

### Вариант 3: Ручное создание через SQL

Если нужно создать таблицу напрямую через SQL:

**Шаги:**
1. Подключиться к базе данных:
   ```bash
   sqlite3 docscan.db
   ```
2. Выполнить SQL команду:
   ```sql
   CREATE TABLE IF NOT EXISTS search_bots (
       id INTEGER PRIMARY KEY AUTOINCREMENT,
       ip_address VARCHAR(50) NOT NULL,
       user_agent VARCHAR(500),
       bot_type VARCHAR(50) NOT NULL,
       first_seen VARCHAR(30) NOT NULL,
       last_seen VARCHAR(30) NOT NULL,
       visits_count INTEGER DEFAULT 0
   );
   
   CREATE INDEX IF NOT EXISTS ix_search_bots_ip_address ON search_bots(ip_address);
   ```
3. Проверить создание:
   ```sql
   .schema search_bots
   ```
4. Выйти:
   ```sql
   .quit
   ```

## Проверка миграции

После создания таблицы проверьте:

1. **Таблица существует:**
   ```bash
   sqlite3 docscan.db ".tables" | grep search_bots
   ```

2. **Структура таблицы правильная:**
   ```bash
   sqlite3 docscan.db ".schema search_bots"
   ```

3. **Приложение запускается без ошибок:**
   - Проверьте логи приложения
   - Убедитесь что нет ошибок связанных с таблицей `search_bots`

4. **Админ-панель работает:**
   - Зайдите в админ-панель
   - Проверьте что вкладка "🕷️ Поисковые боты" отображается
   - Проверьте что статистика ботов загружается

## Откат миграции (если нужно)

Если нужно откатить изменения:

```sql
DROP TABLE IF EXISTS search_bots;
```

**Внимание:** Это удалит все данные о ботах. Используйте только если действительно нужно откатить изменения.

## Что изменилось в коде

### Новые файлы:
- `utils/bot_detector.py` - утилита для определения ботов
- `EMAIL_VERIFICATION_REQUIREMENT.md` - описание системы подтверждения email

### Измененные файлы:
- `models/sqlite_users.py` - добавлена модель `SearchBot` и методы для работы с ботами
- `routes/api.py` - добавлена проверка и блокировка вредоносных ботов
- `routes/admin.py` - добавлена вкладка для мониторинга ботов
- `models/limits.py` - добавлена проверка ботов перед созданием записи гостя
- `app.py` - обновлен middleware для проверки ботов

## После миграции

После успешной миграции:

1. ✅ Вредоносные боты (got, zgrab, GetIntent) будут блокироваться
2. ✅ Поисковые боты (YandexBot, Googlebot) будут записываться в отдельную таблицу
3. ✅ Статистика гостей будет показывать только реальных пользователей
4. ✅ В админ-панели появится вкладка "🕷️ Поисковые боты" для мониторинга

## Возможные проблемы

### Проблема: Таблица не создается автоматически

**Решение:**
- Убедитесь что используется `db.create_all()` при инициализации приложения
- Или запустите `create_database.py` вручную

### Проблема: Ошибка при импорте SearchBot

**Решение:**
- Убедитесь что код обновлен с GitHub
- Перезапустите приложение

### Проблема: Боты не записываются

**Решение:**
- Проверьте логи приложения на наличие ошибок
- Убедитесь что таблица `search_bots` создана
- Проверьте что `utils/bot_detector.py` доступен

## Дата создания

**Создано:** 2025-01-XX  
**Версия:** 1.0  
**Статус:** Готово к применению

