# 📦 Инструкция по деплою на GitHub и сервер

## ШАГ 1: Заливаем файлы на GitHub

### 1.1. Делаем коммит (сохраняем изменения)

Выполните эти команды по порядку в терминале:

```bash
cd "c:\Users\Radmil\Desktop\docscan lk"
```

```bash
git commit -m "Добавлена система регистрации и авторизации пользователей"
```

Эта команда сохраняет все изменения локально.

### 1.2. Отправляем на GitHub

```bash
git push -u origin main
```

Если Git спросит логин и пароль:
- **Логин**: ваш GitHub username (ramay1243)
- **Пароль**: нужно создать Personal Access Token (см. ниже)

### 1.3. Если нужен Personal Access Token

GitHub не принимает обычные пароли. Нужен токен:

1. Зайдите на https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Название: "docscan-deploy"
4. Выберите срок действия (например, "No expiration")
5. Отметьте галочку **`repo`** (полный доступ к репозиториям)
6. Нажмите "Generate token"
7. **СКОПИРУЙТЕ ТОКЕН** (он показывается только один раз!)
8. Используйте этот токен вместо пароля при `git push`

---

## ШАГ 2: Обновление на сервере

### 2.1. Подключитесь к серверу

Через SSH или используйте ваш метод доступа к серверу.

### 2.2. Перейдите в папку проекта

Обычно это что-то вроде:
```bash
cd /var/www/docscan
```
или
```bash
cd /home/user/docscan-ai
```

Точный путь зависит от вашей настройки сервера.

### 2.3. Получите обновления с GitHub

```bash
git pull origin main
```

Эта команда скачает все новые файлы с GitHub на сервер.

### 2.4. Обновите зависимости Python

```bash
pip install -r requirements.txt
```

Или если используете виртуальное окружение:
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### 2.5. Обновите базу данных

```bash
python create_database.py
```

Это создаст новые поля в таблице users и таблицу analysis_history.

### 2.6. Настройте переменные окружения

Отредактируйте файл с переменными окружения (обычно `.env` или `/var/www/data/.env`):

```bash
nano /var/www/data/.env
```

Добавьте или обновите эти строки:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш_email@gmail.com
SMTP_PASSWORD=ваш_пароль_приложения_от_gmail
FROM_EMAIL=noreply@docscan-ai.ru
BASE_URL=https://docscan-ai.ru
```

**Важно для Gmail:**
- Нужен не обычный пароль, а "Пароль приложения"
- Создать можно здесь: https://myaccount.google.com/apppasswords
- Выберите "Почта" и "Другое устройство", введите название и скопируйте пароль

### 2.7. Перезапустите приложение

Если используете systemd:
```bash
sudo systemctl restart docscan
```

Или если используете gunicorn напрямую:
```bash
pkill -f gunicorn
gunicorn app:app
```

Или если используете другой процесс-менеджер, перезапустите его.

---

## Проверка работы

1. Откройте сайт в браузере
2. Проверьте, что появились кнопки "Войти" и "Регистрация" в шапке
3. Попробуйте зарегистрироваться
4. Проверьте, что приходит email для верификации (может быть в спаме)

---

## Если что-то пошло не так

### Ошибка при git push:
- Проверьте, что Personal Access Token правильный
- Убедитесь, что репозиторий существует: https://github.com/ramay1243/docscan-ai

### Ошибка на сервере:
- Проверьте логи: `tail -f /var/log/docscan/error.log`
- Убедитесь, что все зависимости установлены: `pip list`
- Проверьте, что база данных обновлена: `python check_database.py`

### Email не отправляется:
- Проверьте настройки SMTP в .env файле
- Убедитесь, что используете "Пароль приложения" от Gmail
- Проверьте, что SMTP_HOST и SMTP_PORT правильные

---

## Новые файлы, которые были добавлены:

1. **routes/auth.py** - маршруты регистрации и авторизации
2. **utils/auth.py** - утилиты для работы с паролями
3. **utils/email_service.py** - отправка email
4. **static/templates/register.html** - страница регистрации
5. **static/templates/login.html** - страница входа
6. **static/templates/cabinet.html** - личный кабинет
7. **static/templates/forgot_password.html** - восстановление пароля
8. **static/templates/reset_password.html** - сброс пароля
9. **static/templates/message.html** - страница сообщений

## Измененные файлы:

1. **models/sqlite_users.py** - добавлены поля для регистрации
2. **routes/api.py** - логика проверки бесплатного анализа
3. **routes/main.py** - добавлены кнопки входа/регистрации
4. **app.py** - зарегистрирован новый blueprint auth
5. **requirements.txt** - добавлены flask-sqlalchemy и werkzeug

---

Удачи с деплоем! 🚀

