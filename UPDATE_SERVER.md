# Инструкция по обновлению сервера с GitHub

## ⚠️ ВАЖНО: Перед обновлением сохраните важные файлы!

### Шаг 1: Сохраните конфигурацию и базу данных

```bash
# Подключитесь к серверу
ssh root@ваш_сервер

# Перейдите в папку проекта
cd /var/www/docscan

# Создайте резервную копию важных файлов
mkdir -p /var/www/backup
cp /var/www/data/.env /var/www/backup/.env.backup
cp docscan.db /var/www/backup/docscan.db.backup
```

### Шаг 2: Проверьте текущее состояние

```bash
# Проверьте статус Git
git status

# Проверьте какие файлы изменены локально
git diff
```

### Шаг 3: Сохраните локальные изменения (если есть)

```bash
# Если есть важные локальные изменения, сохраните их
git stash save "Локальные изменения перед обновлением"
```

### Шаг 4: Обновите с GitHub

```bash
# Получите последние изменения
git fetch origin

# Обновите файлы (БЕЗ --hard, чтобы не потерять локальные изменения)
git pull origin main

# Если есть конфликты, разрешите их вручную
# Или если хотите полностью заменить на версию с GitHub:
git reset --hard origin/main
```

### Шаг 5: Восстановите конфигурацию

```bash
# Восстановите .env файл если он был перезаписан
cp /var/www/backup/.env.backup /var/www/data/.env

# Проверьте что файл на месте
cat /var/www/data/.env
```

### Шаг 6: Обновите зависимости Python

```bash
# Установите новые зависимости если они появились
pip3 install -r requirements.txt
```

### Шаг 7: Обновите базу данных (если нужно)

```bash
# Если были изменения в структуре БД, выполните миграции
python3 create_database.py
```

### Шаг 8: Проверьте права доступа

```bash
# Убедитесь что права правильные
sudo chown -R www-data:www-data /var/www/docscan
sudo chmod -R 755 /var/www/docscan
sudo chmod 664 /var/www/docscan/docscan.db
```

### Шаг 9: Перезапустите приложение

```bash
# Перезапустите через supervisor
sudo supervisorctl restart docscan

# Проверьте статус
sudo supervisorctl status docscan

# Проверьте логи
sudo supervisorctl tail -f docscan
```

### Шаг 10: Проверьте работу сайта

```bash
# Проверьте что сайт работает
curl http://localhost:5000

# Или откройте в браузере
# https://docscan-ai.ru
```

---

## 🚨 Если что-то пошло не так

### Откат к предыдущей версии:

```bash
# Посмотрите историю коммитов
git log --oneline -10

# Откатитесь к предыдущему коммиту
git reset --hard HEAD~1

# Или к конкретному коммиту
git reset --hard <commit_hash>

# Перезапустите
sudo supervisorctl restart docscan
```

### Восстановление из резервной копии:

```bash
# Восстановите .env
cp /var/www/backup/.env.backup /var/www/data/.env

# Восстановите базу данных (если нужно)
cp /var/www/backup/docscan.db.backup docscan.db
```

---

## 📝 Быстрая команда для обновления (если все настроено)

```bash
cd /var/www/docscan
git fetch origin
git pull origin main
pip3 install -r requirements.txt
sudo supervisorctl restart docscan
```

---

## ⚠️ ЧТО НЕ ДЕЛАТЬ:

1. ❌ НЕ используйте `git reset --hard` без резервной копии
2. ❌ НЕ удаляйте файл `.env`
3. ❌ НЕ удаляйте базу данных `docscan.db`
4. ❌ НЕ коммитьте `.env` файл в Git (он должен быть в .gitignore)




## ⚠️ ВАЖНО: Перед обновлением сохраните важные файлы!

### Шаг 1: Сохраните конфигурацию и базу данных

```bash
# Подключитесь к серверу
ssh root@ваш_сервер

# Перейдите в папку проекта
cd /var/www/docscan

# Создайте резервную копию важных файлов
mkdir -p /var/www/backup
cp /var/www/data/.env /var/www/backup/.env.backup
cp docscan.db /var/www/backup/docscan.db.backup
```

### Шаг 2: Проверьте текущее состояние

```bash
# Проверьте статус Git
git status

# Проверьте какие файлы изменены локально
git diff
```

### Шаг 3: Сохраните локальные изменения (если есть)

```bash
# Если есть важные локальные изменения, сохраните их
git stash save "Локальные изменения перед обновлением"
```

### Шаг 4: Обновите с GitHub

```bash
# Получите последние изменения
git fetch origin

# Обновите файлы (БЕЗ --hard, чтобы не потерять локальные изменения)
git pull origin main

# Если есть конфликты, разрешите их вручную
# Или если хотите полностью заменить на версию с GitHub:
git reset --hard origin/main
```

### Шаг 5: Восстановите конфигурацию

```bash
# Восстановите .env файл если он был перезаписан
cp /var/www/backup/.env.backup /var/www/data/.env

# Проверьте что файл на месте
cat /var/www/data/.env
```

### Шаг 6: Обновите зависимости Python

```bash
# Установите новые зависимости если они появились
pip3 install -r requirements.txt
```

### Шаг 7: Обновите базу данных (если нужно)

```bash
# Если были изменения в структуре БД, выполните миграции
python3 create_database.py
```

### Шаг 8: Проверьте права доступа

```bash
# Убедитесь что права правильные
sudo chown -R www-data:www-data /var/www/docscan
sudo chmod -R 755 /var/www/docscan
sudo chmod 664 /var/www/docscan/docscan.db
```

### Шаг 9: Перезапустите приложение

```bash
# Перезапустите через supervisor
sudo supervisorctl restart docscan

# Проверьте статус
sudo supervisorctl status docscan

# Проверьте логи
sudo supervisorctl tail -f docscan
```

### Шаг 10: Проверьте работу сайта

```bash
# Проверьте что сайт работает
curl http://localhost:5000

# Или откройте в браузере
# https://docscan-ai.ru
```

---

## 🚨 Если что-то пошло не так

### Откат к предыдущей версии:

```bash
# Посмотрите историю коммитов
git log --oneline -10

# Откатитесь к предыдущему коммиту
git reset --hard HEAD~1

# Или к конкретному коммиту
git reset --hard <commit_hash>

# Перезапустите
sudo supervisorctl restart docscan
```

### Восстановление из резервной копии:

```bash
# Восстановите .env
cp /var/www/backup/.env.backup /var/www/data/.env

# Восстановите базу данных (если нужно)
cp /var/www/backup/docscan.db.backup docscan.db
```

---

## 📝 Быстрая команда для обновления (если все настроено)

```bash
cd /var/www/docscan
git fetch origin
git pull origin main
pip3 install -r requirements.txt
sudo supervisorctl restart docscan
```

---

## ⚠️ ЧТО НЕ ДЕЛАТЬ:

1. ❌ НЕ используйте `git reset --hard` без резервной копии
2. ❌ НЕ удаляйте файл `.env`
3. ❌ НЕ удаляйте базу данных `docscan.db`
4. ❌ НЕ коммитьте `.env` файл в Git (он должен быть в .gitignore)


