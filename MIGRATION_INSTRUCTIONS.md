# Инструкция по миграции: Новая система тарифов

## 📋 Описание изменений

Система тарифов изменена с помесячной подписки на разовые покупки с накопительными анализами:

- **Базовый тариф**: 10 анализов за 290₽ (было: безлимит за 490₽/мес)
- **Премиум тариф**: 30 анализов за 690₽ (новый тариф)
- **Анализы не сгорают**: купленные анализы не имеют срока действия
- **Анализы накапливаются**: при покупке нескольких тарифов анализы суммируются

## 🔧 Шаги миграции на сервере

### Шаг 1: Подключитесь к серверу

```bash
ssh root@ваш_сервер
# или
ssh ваш_пользователь@ваш_сервер
```

### Шаг 2: Перейдите в директорию проекта

```bash
cd /var/www/docscan
# или ваша директория проекта
```

### Шаг 3: Создайте резервную копию базы данных

**ВАЖНО! Обязательно создайте резервную копию перед миграцией!**

```bash
# Найти путь к базе данных
python3 -c "from config import Config; print(Config.SQLALCHEMY_DATABASE_URI.replace('sqlite:///', ''))"

# Создать резервную копию (замените путь на реальный)
cp docscan.db docscan.db.backup_$(date +%Y%m%d_%H%M%S)

# Или если база в другом месте:
cp /var/www/docscan/docscan.db /var/www/docscan/docscan.db.backup_$(date +%Y%m%d_%H%M%S)
```

### Шаг 4: Обновите код с GitHub

```bash
# Проверьте текущую ветку
git branch

# Получите последние изменения
git pull origin main

# Проверьте, что файлы обновились
ls -la migrate_add_available_analyses.py
ls -la models/sqlite_users.py
ls -la config.py
```

### Шаг 5: Остановите приложение

```bash
# Если используете supervisor
sudo supervisorctl stop docscan

# Или если используете systemd
sudo systemctl stop docscan

# Или если запускаете вручную, нажмите Ctrl+C
```

### Шаг 6: Запустите миграцию базы данных

```bash
# Убедитесь, что вы в правильной директории
cd /var/www/docscan

# Запустите миграцию
python3 migrate_add_available_analyses.py
```

**Ожидаемый вывод:**
```
📁 Используется база данных: /var/www/docscan/docscan.db
🔍 Проверка существующих колонок...
→ Добавляем available_analyses...
✅ available_analyses добавлена
✅ Миграция успешно завершена!
```

**Если колонка уже существует:**
```
📁 Используется база данных: /var/www/docscan/docscan.db
🔍 Проверка существующих колонок...
✓ Колонка available_analyses уже существует
✅ Миграция успешно завершена!
```

### Шаг 7: Проверьте миграцию

```bash
# Проверить структуру таблицы users
sqlite3 docscan.db "PRAGMA table_info(users);" | grep available_analyses

# Должно вывести что-то вроде:
# 22|available_analyses|INTEGER|0|0|0
```

### Шаг 8: Обновите существующих пользователей (опционально)

Если у вас есть пользователи с активными платными тарифами, вы можете конвертировать их:

```bash
# Создайте скрипт для конвертации (опционально)
cat > convert_existing_users.py << 'EOF'
#!/usr/bin/env python3
import sys
import os
import sqlite3

project_path = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, project_path)

def get_db_path():
    try:
        from config import Config
        db_uri = Config.SQLALCHEMY_DATABASE_URI
        return db_uri.replace('sqlite:///', '')
    except Exception:
        return os.path.join(project_path, "docscan.db")

def convert_users():
    db_path = get_db_path()
    print(f"📁 Используется база данных: {db_path}")
    
    conn = sqlite3.connect(db_path)
    cursor = conn.cursor()
    
    # Найти пользователей с активными платными тарифами
    cursor.execute("SELECT user_id, plan, plan_expires FROM users WHERE plan IN ('basic', 'premium') AND plan_expires IS NOT NULL")
    users = cursor.fetchall()
    
    if not users:
        print("✅ Нет пользователей для конвертации")
        return
    
    print(f"📊 Найдено пользователей для конвертации: {len(users)}")
    
    # Конвертируем: basic = 10 анализов, premium = 30 анализов
    converted = 0
    for user_id, plan, plan_expires in users:
        if plan == 'basic':
            analyses_to_add = 10
        elif plan == 'premium':
            analyses_to_add = 30
        else:
            continue
        
        # Получить текущий баланс
        cursor.execute("SELECT available_analyses FROM users WHERE user_id = ?", (user_id,))
        result = cursor.fetchone()
        current_balance = result[0] if result and result[0] is not None else 0
        
        # Добавить анализы
        new_balance = current_balance + analyses_to_add
        
        cursor.execute("""
            UPDATE users 
            SET available_analyses = ?, 
                plan_expires = NULL 
            WHERE user_id = ?
        """, (new_balance, user_id))
        
        converted += 1
        print(f"✅ Пользователь {user_id}: добавлено {analyses_to_add} анализов, баланс: {new_balance}")
    
    conn.commit()
    conn.close()
    
    print(f"✅ Конвертировано пользователей: {converted}")

if __name__ == '__main__':
    convert_users()
EOF

# Запустите конвертацию (опционально)
python3 convert_existing_users.py
```

**Примечание:** Этот шаг опционален. Если не запускать конвертацию, существующие пользователи с активными тарифами сохранят свой статус, но при следующей покупке тарифа анализы будут добавляться к балансу.

### Шаг 9: Перезапустите приложение

```bash
# Если используете supervisor
sudo supervisorctl start docscan
sudo supervisorctl status docscan

# Или если используете systemd
sudo systemctl start docscan
sudo systemctl status docscan
```

### Шаг 10: Проверьте логи

```bash
# Проверьте логи приложения
tail -f /var/log/docscan.out.log

# Или если используете journalctl
journalctl -u docscan -f
```

**Что искать в логах:**
- ✅ Успешный запуск приложения
- ✅ Нет ошибок "no such column: available_analyses"
- ✅ Нет ошибок при загрузке пользователей

### Шаг 11: Проверьте работу системы

1. **Проверьте главную страницу:**
   - Откройте https://docscan-ai.ru
   - Проверьте, что тарифы отображаются с новыми ценами (290₽ и 690₽)

2. **Проверьте страницу тарифов:**
   - Откройте https://docscan-ai.ru/tariffs
   - Убедитесь, что страница загружается корректно

3. **Проверьте личный кабинет:**
   - Войдите в аккаунт
   - Проверьте, что отображается количество анализов (если есть платный тариф)

4. **Проверьте покупку тарифа:**
   - Попробуйте купить тариф (тестовый платеж)
   - Убедитесь, что анализы добавляются к балансу

## 🔍 Проверка миграции

### Проверка структуры базы данных

```bash
sqlite3 docscan.db "PRAGMA table_info(users);"
```

Должна быть колонка:
```
22|available_analyses|INTEGER|0|0|0
```

### Проверка данных пользователей

```bash
# Проверить пользователей с платными тарифами
sqlite3 docscan.db "SELECT user_id, plan, available_analyses, plan_expires FROM users WHERE plan != 'free' LIMIT 5;"
```

## ⚠️ Откат изменений (если что-то пошло не так)

Если возникли проблемы, можно откатить изменения:

```bash
# 1. Остановите приложение
sudo supervisorctl stop docscan

# 2. Восстановите резервную копию базы данных
cp docscan.db.backup_YYYYMMDD_HHMMSS docscan.db

# 3. Откатите код на предыдущую версию
git log --oneline -10  # Найдите нужный коммит
git checkout <предыдущий_коммит>

# 4. Перезапустите приложение
sudo supervisorctl start docscan
```

## 📝 Важные замечания

1. **Резервная копия обязательна** - всегда создавайте backup перед миграцией
2. **Тестирование** - протестируйте на тестовом сервере перед продакшеном
3. **Мониторинг** - следите за логами после миграции
4. **Существующие пользователи** - пользователи с активными тарифами сохранят доступ, но при следующей покупке система переключится на новую модель

## 🆘 Решение проблем

### Проблема: "no such column: available_analyses"

**Решение:**
```bash
# Убедитесь, что миграция выполнена
python3 migrate_add_available_analyses.py

# Проверьте, что колонка добавлена
sqlite3 docscan.db "PRAGMA table_info(users);" | grep available_analyses
```

### Проблема: "database is locked"

**Решение:**
```bash
# Остановите приложение перед миграцией
sudo supervisorctl stop docscan

# Подождите несколько секунд
sleep 5

# Запустите миграцию
python3 migrate_add_available_analyses.py
```

### Проблема: Пользователи не видят анализы

**Решение:**
```bash
# Проверьте, что колонка существует и имеет правильные значения
sqlite3 docscan.db "SELECT user_id, plan, available_analyses FROM users WHERE user_id = 'USER_ID';"

# Если available_analyses NULL, обновите вручную
sqlite3 docscan.db "UPDATE users SET available_analyses = 0 WHERE available_analyses IS NULL;"
```

## ✅ Чеклист миграции

- [ ] Создана резервная копия базы данных
- [ ] Код обновлен с GitHub (git pull)
- [ ] Приложение остановлено
- [ ] Миграция выполнена успешно
- [ ] Колонка `available_analyses` добавлена в таблицу `users`
- [ ] Приложение перезапущено
- [ ] Логи проверены (нет ошибок)
- [ ] Главная страница работает
- [ ] Страница `/tariffs` работает
- [ ] Личный кабинет отображает анализы корректно
- [ ] Покупка тарифа работает

## 📞 Поддержка

Если возникли проблемы, проверьте:
1. Логи приложения: `/var/log/docscan.out.log`
2. Логи supervisor: `sudo supervisorctl tail -f docscan`
3. Структуру базы данных: `sqlite3 docscan.db ".schema users"`

