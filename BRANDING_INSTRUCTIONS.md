# 🎨 Инструкция по использованию кастомного брендинга

## Описание

Кастомный брендинг позволяет пользователям бизнес-тарифов настраивать внешний вид отчетов (PDF, Word, Excel) с использованием собственного логотипа и корпоративных цветов.

## Возможности

- ✅ Загрузка логотипа компании
- ✅ Настройка основного цвета (для заголовков, таблиц)
- ✅ Настройка вторичного цвета (для акцентов)
- ✅ Указание названия компании (отображается в заголовках и футерах)
- ✅ Применение ко всем форматам экспорта (PDF, Word, Excel)

## Как использовать

### 1. Через админ-панель

1. Войдите в админ-панель: `/admin`
2. Перейдите в раздел **"👥 Пользователи"**
3. Найдите секцию **"🎨 Кастомный брендинг"**
4. Введите **ID пользователя**, для которого нужно настроить брендинг
5. Нажмите **"📥 Загрузить настройки"** (если настройки уже есть) или заполните форму для создания новых
6. Заполните форму:
   - **Название компании** - будет отображаться в заголовках отчетов
   - **Основной цвет** - цвет заголовков и таблиц (формат: #4361ee)
   - **Вторичный цвет** - цвет акцентов (формат: #764ba2)
   - **Логотип** - загрузите изображение (PNG, JPG, JPEG, GIF, SVG, макс. 2MB)
7. Нажмите **"💾 Сохранить настройки"**

### 2. Через API (для разработчиков)

#### Получить настройки брендинга:
```bash
GET /admin/get-branding?user_id=USER_ID
```

#### Сохранить настройки:
```bash
POST /admin/save-branding
Content-Type: multipart/form-data

user_id: USER_ID
company_name: ООО Рога и Копыта
primary_color: #4361ee
secondary_color: #764ba2
logo: [файл изображения]
```

#### Включить/выключить брендинг:
```bash
POST /admin/toggle-branding
Content-Type: application/json

{
  "user_id": "USER_ID",
  "is_active": true
}
```

#### Удалить брендинг:
```bash
POST /admin/delete-branding
Content-Type: application/json

{
  "user_id": "USER_ID"
}
```

## Требования к логотипу

- **Форматы**: PNG, JPG, JPEG, GIF, SVG
- **Рекомендуемый размер**: 200x80px
- **Максимальный размер файла**: 2MB
- **Рекомендации**: 
  - Используйте прозрачный фон (PNG) для лучшего отображения
  - Логотип должен быть четким и читаемым в небольшом размере
  - Предпочтительно горизонтальная ориентация

## Где применяется брендинг

### PDF отчеты:
- Логотип отображается в верхней части документа
- Название компании используется как заголовок
- Основной цвет применяется к заголовкам и таблицам
- Вторичный цвет используется для акцентов
- Название компании отображается в футере

### Word отчеты:
- Логотип вставляется в начало документа
- Название компании в заголовке
- Цвета применяются к тексту и форматированию

### Excel отчеты:
- Название компании в заголовке таблицы
- Основной цвет для заголовков таблиц
- Цвета применяются к ячейкам и форматированию

## Миграция базы данных

Перед использованием необходимо выполнить миграцию:

```bash
python migrate_add_branding_settings.py
```

Это создаст таблицу `branding_settings` в базе данных.

## Структура таблицы

```sql
CREATE TABLE branding_settings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id VARCHAR(8) NOT NULL UNIQUE,
    logo_path VARCHAR(500),
    primary_color VARCHAR(7) DEFAULT '#4361ee',
    secondary_color VARCHAR(7) DEFAULT '#764ba2',
    company_name VARCHAR(255),
    is_active BOOLEAN DEFAULT 1,
    created_at VARCHAR(30) NOT NULL,
    updated_at VARCHAR(30),
    FOREIGN KEY (user_id) REFERENCES users(user_id)
);
```

## Примеры использования

### Пример 1: Настройка для юридической фирмы
- **Название**: ООО "Юридическая фирма"
- **Основной цвет**: #1a365d (темно-синий)
- **Вторичный цвет**: #2c5282 (синий)
- **Логотип**: logo_law_firm.png

### Пример 2: Настройка для банка
- **Название**: Банк "Финансы"
- **Основной цвет**: #c53030 (красный)
- **Вторичный цвет**: #e53e3e (светло-красный)
- **Логотип**: logo_bank.png

## Примечания

- Брендинг применяется только для пользователей с активными настройками (`is_active = true`)
- Если брендинг не настроен или деактивирован, используются стандартные цвета и стили
- Логотип сохраняется в папке `static/logos/` с уникальным именем
- При удалении брендинга логотип также удаляется с сервера
- Старый логотип автоматически удаляется при загрузке нового

## Технические детали

- Логотипы хранятся в: `static/logos/`
- Формат имен файлов: `{user_id}_{uuid}.{ext}`
- Цвета должны быть в формате HEX (#RRGGBB)
- Все настройки привязаны к конкретному пользователю (user_id)

