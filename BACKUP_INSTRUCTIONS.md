# Инструкция по настройке автоматического резервного копирования

## 📦 Что делает система бэкапов

Система автоматически создает резервные копии базы данных SQLite в сжатом формате (GZIP), сохраняет их в папке `backups/` и автоматически удаляет старые бэкапы.

---

## 🚀 Быстрый старт

### 1. Создать первый бэкап вручную

```bash
cd /var/www/docscan
python backup_database.py
```

Должно появиться: `✅ Бэкап успешно создан`

### 2. Проверить список бэкапов

```bash
python backup_database.py --list
```

### 3. Настроить автоматические бэкапы (cron)

---

## ⚙️ Настройка автоматических бэкапов через cron

### Вариант 1: Ежедневный бэкап в 3:00 ночи

```bash
# Откройте crontab
crontab -e

# Добавьте строку (замените /var/www/docscan на ваш путь):
0 3 * * * cd /var/www/docscan && /usr/bin/python3 backup_database.py >> /var/www/docscan/backup.log 2>&1
```

### Вариант 2: Бэкап каждые 6 часов

```bash
0 */6 * * * cd /var/www/docscan && /usr/bin/python3 backup_database.py >> /var/www/docscan/backup.log 2>&1
```

### Вариант 3: Бэкап дважды в день (утром и вечером)

```bash
0 3,15 * * * cd /var/www/docscan && /usr/bin/python3 backup_database.py >> /var/www/docscan/backup.log 2>&1
```

### Вариант 4: Если используете виртуальное окружение

```bash
0 3 * * * cd /var/www/docscan && /var/www/docscan/venv/bin/python backup_database.py >> /var/www/docscan/backup.log 2>&1
```

---

## 📋 Формат cron

```
минута час день месяц день_недели команда
```

Примеры:
- `0 3 * * *` - каждый день в 3:00
- `0 */6 * * *` - каждые 6 часов
- `0 3,15 * * *` - в 3:00 и 15:00 каждый день
- `0 3 * * 0` - каждое воскресенье в 3:00

---

## 🗂️ Структура папки backups

После создания бэкапов структура будет такой:

```
/var/www/docscan/
├── backups/
│   ├── docscan_backup_20250115_030000.db.gz
│   ├── docscan_backup_20250116_030000.db.gz
│   ├── docscan_backup_20250117_030000.db.gz
│   └── ...
├── backup_database.py
├── restore_database.py
└── docscan.db
```

---

## 🔄 Восстановление из бэкапа

### Интерактивный режим (рекомендуется)

```bash
cd /var/www/docscan
python restore_database.py
```

Скрипт покажет список всех бэкапов, вы выберете нужный, и он восстановится.

### Восстановление конкретного файла

```bash
python restore_database.py --file docscan_backup_20250115_030000.db.gz
```

---

## 🧹 Очистка старых бэкапов

### Автоматическая очистка

Старые бэкапы (старше 30 дней) автоматически удаляются при создании нового бэкапа.

### Ручная очистка

```bash
python backup_database.py --clean
```

### Ограничение количества

Система автоматически ограничивает количество бэкапов до 50 (удаляет самые старые при превышении).

---

## 📊 Информация о бэкапах

```bash
python backup_database.py --info
```

Покажет:
- Всего бэкапов
- Общий размер
- Самый старый бэкап
- Самый новый бэкап

---

## ⚠️ Важные замечания

1. **Перед восстановлением** скрипт автоматически создает резервную копию текущей БД как `docscan.db.backup_before_restore`

2. **Проверьте путь к Python** в cron:
   ```bash
   which python3
   # или
   which python
   ```

3. **Проверьте права доступа**:
   ```bash
   chmod +x backup_database.py
   chmod +x restore_database.py
   ```

4. **Проверьте логи**:
   ```bash
   tail -f /var/www/docscan/backup.log
   ```

---

## 🔐 Безопасность

1. **Храните бэкапы в безопасном месте** - папка `backups/` должна быть защищена от публичного доступа

2. **Регулярно проверяйте бэкапы** - убедитесь, что они создаются успешно

3. **Тестируйте восстановление** - периодически проверяйте, что бэкапы можно восстановить

4. **Храните бэкапы вне сервера** - для максимальной безопасности копируйте бэкапы в облако или на другой сервер

---

## 📈 Рекомендации

1. **Ежедневные бэкапы** - минимум для продакшена
2. **Хранить 30 дней** - достаточно для большинства случаев
3. **Проверять еженедельно** - убедиться, что бэкапы создаются
4. **Тестировать восстановление** - раз в месяц проверять, что бэкапы работают

---

## 🆘 Устранение проблем

### Бэкапы не создаются

1. Проверьте права доступа:
   ```bash
   ls -la backup_database.py
   chmod +x backup_database.py
   ```

2. Проверьте путь к Python в cron

3. Проверьте логи:
   ```bash
   cat backup.log
   ```

### Ошибка "Permission denied"

```bash
chmod 755 backup_database.py
chmod 755 restore_database.py
```

### Бэкапы слишком большие

Это нормально для SQLite. Сжатие уменьшает размер примерно в 3-5 раз.

---

## ✅ Проверка работы

После настройки cron проверьте:

1. Дождитесь времени следующего бэкапа или создайте вручную
2. Проверьте наличие файлов:
   ```bash
   ls -lh backups/
   ```
3. Проверьте логи:
   ```bash
   tail backup.log
   ```

---

Готово! Система резервного копирования настроена и работает автоматически.

