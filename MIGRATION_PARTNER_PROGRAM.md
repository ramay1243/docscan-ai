# Инструкция по миграции базы данных для партнерской программы

## Что добавлено

Партнерская программа добавляет следующие таблицы и поля:

### Новые таблицы:
1. **`referrals`** - хранит информацию о приглашениях (кто пригласил, кого пригласили)
2. **`referral_rewards`** - хранит информацию о вознаграждениях партнеров

### Новые поля в таблице `users`:
1. **`referral_code`** - уникальный реферальный код пользователя
2. **`referrer_id`** - ID пользователя, который пригласил (может быть NULL)
3. **`payment_details`** - реквизиты для получения выплат (JSON, может быть NULL)

## Инструкция по миграции на сервере

### Шаг 1: Обновить код

```bash
cd /var/www/docscan  # или путь к вашему проекту
git pull origin main
```

### Шаг 2: Создать новые таблицы

Запустите скрипт создания таблиц:

```bash
python3 create_database.py
```

Или вручную через Python:

```bash
python3 -c "
from app import app, db
with app.app_context():
    db.create_all()
    print('✅ Таблицы созданы')
"
```

### Шаг 3: Проверить создание таблиц

Проверьте, что таблицы созданы:

```bash
sqlite3 /var/www/docscan/instance/users.db ".tables"
```

Должны появиться таблицы:
- `referrals`
- `referral_rewards`

Также проверьте, что в таблице `users` появились новые поля:

```bash
sqlite3 /var/www/docscan/instance/users.db ".schema users"
```

Должны быть поля:
- `referral_code`
- `referrer_id`
- `payment_details`

### Шаг 4: Перезапустить приложение

```bash
# Если используете systemd
sudo systemctl restart docscan

# Или если используете supervisor
sudo supervisorctl restart docscan

# Или если запускаете вручную - перезапустите процесс
```

### Шаг 5: Проверить работу

1. Зайдите в админ-панель: `/admin`
2. Откройте раздел "🎁 Партнерская программа"
3. Проверьте, что секции загружаются без ошибок

## Ручная миграция (если автоматическая не сработала)

Если `db.create_all()` не создал таблицы автоматически, выполните SQL вручную:

```sql
-- Создание таблицы referrals
CREATE TABLE IF NOT EXISTS referrals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    referrer_id VARCHAR(8) NOT NULL,
    invited_user_id VARCHAR(8) NOT NULL,
    created_at VARCHAR(30) NOT NULL,
    registered_at VARCHAR(30),
    FOREIGN KEY (referrer_id) REFERENCES users(user_id),
    FOREIGN KEY (invited_user_id) REFERENCES users(user_id)
);

-- Создание таблицы referral_rewards
CREATE TABLE IF NOT EXISTS referral_rewards (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    partner_id VARCHAR(8) NOT NULL,
    invited_user_id VARCHAR(8) NOT NULL,
    payment_id INTEGER,
    purchase_amount REAL NOT NULL,
    reward_amount REAL NOT NULL,
    reward_percent REAL DEFAULT 15.0,
    status VARCHAR(20) DEFAULT 'pending',
    paid_at VARCHAR(30),
    created_at VARCHAR(30) NOT NULL,
    notes TEXT,
    FOREIGN KEY (partner_id) REFERENCES users(user_id),
    FOREIGN KEY (invited_user_id) REFERENCES users(user_id),
    FOREIGN KEY (payment_id) REFERENCES payments(id)
);

-- Добавление новых полей в таблицу users (если их еще нет)
-- Проверьте сначала, есть ли уже эти поля:
-- sqlite3 /var/www/docscan/instance/users.db ".schema users"

-- Если полей нет, добавьте их:
ALTER TABLE users ADD COLUMN referral_code VARCHAR(20) UNIQUE;
ALTER TABLE users ADD COLUMN referrer_id VARCHAR(8);
ALTER TABLE users ADD COLUMN payment_details TEXT;

-- Добавьте внешний ключ для referrer_id
-- SQLite не поддерживает ALTER TABLE для добавления FOREIGN KEY,
-- поэтому если нужно, создайте таблицу заново с миграцией данных
```

## Важные замечания

1. **Резервная копия**: Перед миграцией сделайте резервную копию БД:
   ```bash
   cp /var/www/docscan/instance/users.db /var/www/docscan/instance/users.db.backup
   ```

2. **Существующие пользователи**: Существующие пользователи автоматически получат `referral_code` при первом открытии раздела "Партнерская программа" в личном кабинете.

3. **Внешние ключи**: SQLite имеет ограниченную поддержку внешних ключей. Убедитесь, что они включены в вашем приложении (обычно включены по умолчанию в Flask-SQLAlchemy).

4. **Проверка логов**: После миграции проверьте логи на наличие ошибок:
   ```bash
   tail -f /var/log/docscan.out.log
   ```

## Откат миграции (если что-то пошло не так)

Если нужно откатить изменения:

```bash
# Восстановить резервную копию БД
cp /var/www/docscan/instance/users.db.backup /var/www/docscan/instance/users.db

# Откатить код
git checkout HEAD~1

# Перезапустить приложение
sudo systemctl restart docscan
```

## Проверка работоспособности

После миграции проверьте:

1. ✅ Админ-панель → Партнерская программа → загружаются данные
2. ✅ Личный кабинет → Партнерская программа → генерируется реферальная ссылка
3. ✅ Страница `/partners` открывается
4. ✅ Регистрация с параметром `?ref=CODE` сохраняет реферальный код
5. ✅ Покупка тарифа приглашенным пользователем создает вознаграждение

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `/var/log/docscan.out.log`
2. Проверьте структуру БД: `sqlite3 /var/www/docscan/instance/users.db ".schema"`
3. Убедитесь, что все зависимости установлены: `pip3 list | grep -i sqlalchemy`


