# Инструкция по установке библиотеки reportlab

## Шаг 1: Загрузка изменений на GitHub

### На локальном компьютере (Windows):

1. Запустите файл: `push_analysis_improvements.bat`
   
   ИЛИ выполните команды вручную:
   
   ```cmd
   cd "C:\Users\Radmil\Desktop\docscan banner"
   git add routes/api.py services/analysis.py routes/main.py config.py services/yandex_gpt.py services/file_processing.py services/pdf_generator.py requirements.txt
   git commit -m "Улучшение анализа: краткий анализ для гостей, расширенный для зарегистрированных, исправление определения типа документа, чтение всех страниц PDF, функция скачивания анализа в PDF"
   git push origin main
   ```

---

## Шаг 2: Обновление на сервере

### Подключитесь к серверу по SSH:

```bash
ssh root@ваш_сервер
```

### Перейдите в директорию проекта:

```bash
cd /var/www/docscan
```

### Обновите код с GitHub:

```bash
git pull origin main
```

---

## Шаг 3: Установка библиотеки reportlab

### Вариант 1: Если используете виртуальное окружение (рекомендуется):

```bash
# Активируйте виртуальное окружение (если есть)
source venv/bin/activate

# Установите библиотеку
pip install reportlab==4.0.7

# Или обновите все зависимости
pip install -r requirements.txt
```

### Вариант 2: Если НЕ используете виртуальное окружение:

```bash
# Установите библиотеку глобально
pip3 install reportlab==4.0.7

# Или обновите все зависимости
pip3 install -r requirements.txt
```

### Вариант 3: Если используете supervisor/gunicorn:

```bash
# Установите библиотеку
pip3 install reportlab==4.0.7

# Перезапустите приложение
sudo supervisorctl restart all
# или
sudo supervisorctl restart docscan
```

---

## Шаг 4: Проверка установки

### Проверьте, что библиотека установлена:

```bash
python3 -c "import reportlab; print('✅ reportlab установлен, версия:', reportlab.Version)"
```

### Если ошибка "ModuleNotFoundError":

```bash
# Убедитесь, что используете правильный Python
which python3
which pip3

# Установите для конкретного Python
python3 -m pip install reportlab==4.0.7
```

---

## Шаг 5: Перезапуск приложения

### Если используете supervisor:

```bash
sudo supervisorctl restart all
```

### Если используете systemd:

```bash
sudo systemctl restart docscan
```

### Если запускаете вручную:

```bash
# Остановите текущий процесс (Ctrl+C)
# Запустите заново
python3 app.py
# или
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

---

## Шаг 6: Проверка работы

1. Откройте сайт в браузере
2. Зарегистрируйтесь или войдите
3. Загрузите документ для анализа
4. После получения расширенного анализа нажмите кнопку "📥 Скачать анализ в PDF"
5. Должен скачаться PDF файл с результатами

---

## Возможные проблемы и решения

### Проблема: "ModuleNotFoundError: No module named 'reportlab'"

**Решение:**
```bash
# Установите библиотеку для того же Python, который использует приложение
python3 -m pip install reportlab==4.0.7

# Проверьте путь к Python в supervisor/gunicorn
cat /etc/supervisor/conf.d/docscan.conf
# или
ps aux | grep python
```

### Проблема: "Permission denied"

**Решение:**
```bash
# Используйте sudo (если нужно)
sudo pip3 install reportlab==4.0.7

# Или установите для пользователя
pip3 install --user reportlab==4.0.7
```

### Проблема: "Command 'pip' not found"

**Решение:**
```bash
# Используйте pip3 вместо pip
pip3 install reportlab==4.0.7

# Или установите pip
sudo apt-get install python3-pip
```

---

## Готово! ✅

После выполнения всех шагов функция скачивания анализа в PDF должна работать.



## Шаг 1: Загрузка изменений на GitHub

### На локальном компьютере (Windows):

1. Запустите файл: `push_analysis_improvements.bat`
   
   ИЛИ выполните команды вручную:
   
   ```cmd
   cd "C:\Users\Radmil\Desktop\docscan banner"
   git add routes/api.py services/analysis.py routes/main.py config.py services/yandex_gpt.py services/file_processing.py services/pdf_generator.py requirements.txt
   git commit -m "Улучшение анализа: краткий анализ для гостей, расширенный для зарегистрированных, исправление определения типа документа, чтение всех страниц PDF, функция скачивания анализа в PDF"
   git push origin main
   ```

---

## Шаг 2: Обновление на сервере

### Подключитесь к серверу по SSH:

```bash
ssh root@ваш_сервер
```

### Перейдите в директорию проекта:

```bash
cd /var/www/docscan
```

### Обновите код с GitHub:

```bash
git pull origin main
```

---

## Шаг 3: Установка библиотеки reportlab

### Вариант 1: Если используете виртуальное окружение (рекомендуется):

```bash
# Активируйте виртуальное окружение (если есть)
source venv/bin/activate

# Установите библиотеку
pip install reportlab==4.0.7

# Или обновите все зависимости
pip install -r requirements.txt
```

### Вариант 2: Если НЕ используете виртуальное окружение:

```bash
# Установите библиотеку глобально
pip3 install reportlab==4.0.7

# Или обновите все зависимости
pip3 install -r requirements.txt
```

### Вариант 3: Если используете supervisor/gunicorn:

```bash
# Установите библиотеку
pip3 install reportlab==4.0.7

# Перезапустите приложение
sudo supervisorctl restart all
# или
sudo supervisorctl restart docscan
```

---

## Шаг 4: Проверка установки

### Проверьте, что библиотека установлена:

```bash
python3 -c "import reportlab; print('✅ reportlab установлен, версия:', reportlab.Version)"
```

### Если ошибка "ModuleNotFoundError":

```bash
# Убедитесь, что используете правильный Python
which python3
which pip3

# Установите для конкретного Python
python3 -m pip install reportlab==4.0.7
```

---

## Шаг 5: Перезапуск приложения

### Если используете supervisor:

```bash
sudo supervisorctl restart all
```

### Если используете systemd:

```bash
sudo systemctl restart docscan
```

### Если запускаете вручную:

```bash
# Остановите текущий процесс (Ctrl+C)
# Запустите заново
python3 app.py
# или
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

---

## Шаг 6: Проверка работы

1. Откройте сайт в браузере
2. Зарегистрируйтесь или войдите
3. Загрузите документ для анализа
4. После получения расширенного анализа нажмите кнопку "📥 Скачать анализ в PDF"
5. Должен скачаться PDF файл с результатами

---

## Возможные проблемы и решения

### Проблема: "ModuleNotFoundError: No module named 'reportlab'"

**Решение:**
```bash
# Установите библиотеку для того же Python, который использует приложение
python3 -m pip install reportlab==4.0.7

# Проверьте путь к Python в supervisor/gunicorn
cat /etc/supervisor/conf.d/docscan.conf
# или
ps aux | grep python
```

### Проблема: "Permission denied"

**Решение:**
```bash
# Используйте sudo (если нужно)
sudo pip3 install reportlab==4.0.7

# Или установите для пользователя
pip3 install --user reportlab==4.0.7
```

### Проблема: "Command 'pip' not found"

**Решение:**
```bash
# Используйте pip3 вместо pip
pip3 install reportlab==4.0.7

# Или установите pip
sudo apt-get install python3-pip
```

---

## Готово! ✅

После выполнения всех шагов функция скачивания анализа в PDF должна работать.

